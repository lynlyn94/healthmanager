ji xu# 康复科管理系统 — 需求分析报告

> 版本: v1.2 | 日期: 2026-06-05 | 作者: WorkBuddy

---

## 目录

1. [系统概述](#1-系统概述)
2. [角色与权限分析](#2-角色与权限分析)
3. [板块一：治疗师/护士端功能详解](#3-板块一治疗师护士端功能详解)
4. [板块二：医生端功能详解](#4-板块二医生端功能详解)
5. [板块三：管理员端功能详解](#5-板块三管理员端功能详解)
6. [模块间关联关系与数据流](#6-模块间关联关系与数据流)
7. [数据库设计概要](#7-数据库设计概要)
8. [技术架构方案](#8-技术架构方案)
9. [UI/UX 设计参考](#9-uiux-设计参考)
10. [接口设计概要](#10-接口设计概要)
11. [开发里程碑建议](#11-开发里程碑建议)

---

## 1. 系统概述

### 1.1 项目背景

康复科管理系统是一套面向医院康复科的数字化管理平台。康复科不同于普通临床科室，治疗以**长期康复训练**为主，涉及多位治疗师/护士协作、多轮次治疗任务的排期与执行跟踪。系统旨在打通"评估 → 方案制定 → 医嘱下达 → 患者日程排程 → 任务执行 → 核销结算 → 工作量统计"的完整闭环。

### 1.2 系统目标

| 目标 | 描述 |
|------|------|
| 任务闭环 | 治疗任务从创建到核销全生命周期管理 |
| 角色协同 | 医生、治疗师、护士、管理员四角色无缝协作 |
| 数据驱动 | 工作量自动统计，支持绩效考核 |
| 信息聚合 | 患者信息集中管理，治疗全流程可追溯 |

### 1.3 用户角色概览

```
┌──────────────────────────────────────────────────────────────┐
│                    康复科管理系统 — 四角色                      │
├──────────┬─────────────┬──────────────┬───────────────────────┤
│  治疗师   │    护士      │    医生       │      管理员           │
│ 核心用户  │  核心用户     │  审批决策     │   系统维护             │
├──────────┼─────────────┼──────────────┼───────────────────────┤
│ 制定方案  │  协助执行    │ 查看治疗方案   │  用户管理(CRUD)        │
│ 执行治疗  │  护理记录    │ 下达医嘱      │  科室管理              │
│ 任务核销  │  量表填写    │ 查看患者      │  字典维护              │
│ 工作量看板 │ 工作量看板   │              │  日志审计              │
└──────────┴─────────────┴──────────────┴───────────────────────┘
```

---

## 2. 角色与权限分析

### 2.1 角色权限矩阵

| 功能模块 | 治疗师 | 护士 | 医生 | 管理员 |
|---------|:-----:|:---:|:---:|:-----:|
| 治疗任务 - 查看 | ✅ | ✅ | ❌ | ✅(全部) |
| 治疗任务 - 核销 | ✅(本人) | ✅(本人) | ❌ | ❌ |
| 治疗任务 - 撤销核销 | ✅(本人) | ✅(本人) | ❌ | ❌ |
| 患者列表 - 查看 | ✅ | ✅ | ✅ | ✅(全部) |
| 患者列表 - 9项操作 | ✅ | ✅(部分) | ✅(医嘱) | ✅ |
| 工作量统计 | ✅(本人) | ✅(本人) | ❌ | ✅(全部) |
| 医嘱管理 | ❌(只读) | ❌(只读) | ✅ | ✅ |
| 功能广场 | ✅ | ✅ | ✅ | ✅ |
| 系统管理(CRUD) | ❌ | ❌ | ❌ | ✅ |

### 2.2 权限控制设计要点

- **数据隔离**: 治疗师/护士默认仅看到分配给自己的患者和任务
- **小组共享**: 支持按治疗小组查看同组成员的患者和任务
- **管理员全域**: 管理员可查看和操作所有数据，但无核销权限
- **操作权限**: 核销与撤销核销仅限任务执行者本人

---

## 3. 板块一：治疗师/护士端功能详解

治疗师与护士共用此板块，是系统的**核心业务板块**。

### 3.1 治疗任务模块

#### 3.1.1 功能描述

治疗师或护士在此模块管理和执行每日治疗任务。

| 编号 | 功能点 | 详细说明 |
|:---:|--------|---------|
| T1 | 当日任务查看 | 默认展示当天自己及同一治疗小组的治疗任务列表 |
| T2 | 日期筛选 | 支持日历选择器，切换查看未来/以往任意日期的任务 |
| T3 | 任务核销 | 对自己的治疗任务进行核销确认（标记完成） |
| T4 | 撤销核销 | 撤销已核销的任务，回退状态（需记录撤销原因） |
| T5 | 任务筛选 | 按患者姓名、任务状态(待执行/执行中/已核销/已撤销)、治疗类型筛选 |

#### 3.1.2 任务状态机

```
  ┌──────────┐    分配    ┌──────────┐   开始执行   ┌──────────┐
  │  待执行   │ ────────→ │  执行中   │ ──────────→ │  已核销   │
  └──────────┘           └──────────┘             └─────┬────┘
       ↑                                                 │
       │                   撤销核销                       │
       │              ┌──────────┐                       │
       └──────────────│  已撤销   │←─────────────────────┘
                      └──────────┘
```

#### 3.1.3 界面原型要点

- 日历组件 + 任务列表双栏布局
- 任务卡片展示：患者姓名、治疗项目、时间段、状态标签
- 核销按钮需二次确认弹窗，防止误操作
- 撤销核销需填写原因（必填）

### 3.2 患者列表模块

#### 3.2.1 功能描述

患者列表是整个板块的**信息中枢**，所有患者相关操作均从此入口发起。

#### 3.2.2 患者筛选条件

| 筛选维度 | 说明 |
|---------|------|
| 默认视图 | 分配给当前登录用户的患者 |
| 本组患者 | 同一治疗小组的全部患者 |
| 其他组患者 | 其他治疗小组的患者（只读） |
| 全部患者 | 康复科所有在院患者 |
| 关键字搜索 | 姓名 / 住院号 / 床号 模糊搜索 |
| 状态筛选 | 在院 / 出院 / 全部 |

#### 3.2.3 患者卡片 9 项操作

点击患者后，展示以下 9 个操作入口：

| 编号 | 操作项 | 详细说明 | 权限 |
|:---:|--------|---------|:---:|
| 1 | **患者详情** | 基本信息、入院信息、诊断信息、过敏史等 | 只读 |
| 2 | **编辑患者** | 修改患者基本信息（联系方式、紧急联系人等） | 治疗师 |
| 3 | **量表评估** | 填写/查看各类康复评估量表（FMA、Barthel等） | 治疗师/护士 |
| 4 | **治疗目标** | 设定短期/长期康复目标，关联治疗方案 | 治疗师 |
| 5 | **治疗方案** | 制定/修改患者的治疗计划（项目、频次、周期） | 治疗师 |
| 6 | **患者医嘱** | 查看医生下达的医嘱列表，治疗师可据此执行 | 只读 |
| 7 | **患者日程** | 查看该患者的治疗日程排期（日历视图） | 全部 |
| 8 | **治疗记录** | 查看该患者的历史治疗记录、核销记录 | 全部 |
| 9 | **患者出院** | 办理出院手续，更新患者状态 | 治疗师 |

#### 3.2.4 操作间的关系链

```
量表评估 ──→ 治疗目标 ──→ 治疗方案 ──→ 医生审阅 ──→ 医嘱
                                              │
                                     ┌────────┘
                                     ↓
                    治疗师/护士在患者日程中查看医嘱
                                     │
                                     ↓
                             根据医嘱进行排程(确认时间段/分配执行人)
                                     │
                                     ↓
                                治疗任务(正式创建)
                                     │
                                     ↓
                               执行治疗
                                     │
                                     ↓
                                任务核销
                                     │
                               ┌─────┴─────┐
                               ↓           ↓
                          治疗记录      工作量统计
                               │
                               ↓
                          患者出院
```

### 3.3 工作量统计模块

#### 3.3.1 功能描述

治疗师或护士完成治疗任务核销后，系统自动将工作量汇总到此模块。

#### 3.3.2 统计维度

| 维度 | 说明 |
|------|------|
| 按日期 | 每日完成治疗项目数、服务患者数 |
| 按周/月 | 周报/月报汇总 |
| 按治疗类型 | 物理治疗/作业治疗/言语治疗等分类统计 |
| 按治疗师/小组 | 个人绩效 vs 小组绩效 |

#### 3.3.3 界面原型要点

- 顶部：数值卡片（今日完成、本周累计、本月累计）
- 中部：折线/柱状图趋势展示
- 底部：明细列表（可追溯至具体核销记录）

### 3.4 功能广场模块

#### 3.4.1 功能描述

所有功能入口的汇总页面，作为快捷导航中心。以图标卡片网格布局展示。

#### 3.4.2 包含入口

- 治疗任务（跳转至 3.1）
- 患者列表（跳转至 3.2）
- 工作量统计（跳转至 3.3）
- 量表评估（快捷入口）
- 患者日程（全局日历视图）
- 医嘱查看
- 个人设置
- 消息通知

---

## 4. 板块二：医生端功能详解

### 4.1 医嘱模块

#### 4.1.1 功能描述

医生在此模块查看治疗师制定的治疗方案，审阅后开具医嘱。**医嘱是治疗任务生成的触发器。**

#### 4.1.2 核心操作

| 操作 | 说明 |
|------|------|
| 查看治疗方案 | 治疗师提交的治疗方案列表，待审阅状态 |
| 审核通过 | 审阅通过后更新患者日程，WebSocket 通知治疗师/护士按医嘱排程 |
| 审核退回 | 退回给治疗师修改，需填写退回原因 |
| 开具医嘱 | 创建新的医嘱单（治疗项目、频次、周期、备注） |
| 修改/作废医嘱 | 修改或作废已有医嘱（需留痕） |
| 医嘱模板 | 常用医嘱模板快速开具 |

#### 4.1.3 WebSocket 推送场景

| 事件 | 推送对象 |
|------|---------|
| 医嘱审核通过 | 通知对应治疗师/护士（"XXX患者医嘱已通过"） |
| 医嘱退回 | 通知治疗方案制定者 |
| 新医嘱下达 | 通知对应治疗小组成员 |

### 4.2 患者列表模块

与治疗师端的患者列表共享数据源，医生视角：
- 查看全部在院患者
- 点击患者可查看：患者详情、量表评估、治疗目标、治疗方案、患者医嘱（可编辑）、治疗记录
- **医生可操作**：患者医嘱（核心）、查看治疗记录
- **医生只读**：量表评估、治疗目标、治疗方案

### 4.3 功能广场

医生版功能广场，入口集合：
- 医嘱管理
- 患者列表
- 待审治疗方案（快捷入口）
- 会诊记录（预留）

---

## 5. 板块三：管理员端功能详解

管理员对系统中所有信息进行维护管理（增删改查）。

### 5.1 管理功能矩阵

| 模块 | 操作 | 说明 |
|------|------|------|
| **用户管理** | CRUD | 管理所有系统用户（治疗师/护士/医生/管理员），分配角色 |
| **科室管理** | CRUD | 康复科内部组织架构 |
| **治疗小组管理** | CRUD | 治疗师分组管理，关联治疗师-小组关系 |
| **患者管理** | CRUD | 全量患者信息维护，包括已出院患者 |
| **治疗项目管理** | CRUD | 维护治疗项目字典（物理治疗、作业治疗、言语治疗等） |
| **量表模板管理** | CRUD | 维护评估量表模板（FMA量表、Barthel指数等） |
| **医嘱模板管理** | CRUD | 维护医生端的常用医嘱模板 |
| **字典管理** | CRUD | 系统级枚举值维护（治疗类型、患者状态、任务状态等） |
| **操作日志** | 查询 | 审计日志查看，支持按用户/时间/操作类型筛选 |
| **数据统计** | 查看 | 全科数据统计看板（各角色工作量、科室运营数据） |

### 5.2 管理员操作约束

- 管理员**不可**进行任务核销（业务操作权限分离）
- 删除操作为**逻辑删除**（软删除），不可物理删除
- 关键操作（用户删除、患者删除）需二次确认
- 所有管理员操作记录至操作日志

---

## 6. 模块间关联关系与数据流

### 6.1 核心业务数据流

```
治疗师/护士端                                医生端
┌──────────────────┐                    ┌──────────────┐
│  量表评估         │                    │              │
│     ↓            │                    │              │
│  治疗目标         │                    │              │
│     ↓            │                    │              │
│  治疗方案 ────────┼──→ 提交审阅 ────→ │  审阅方案    │
│                  │                    │     ↓       │
│                  │                    │  开具医嘱    │
│                  │                    │     ↓       │
│                  │  ←── WebSocket ─── │  审核通过    │
│                  │      通知          └──────────────┘
│  患者医嘱(查看)   │
│     ↓            │
│  患者日程(查看医嘱 │  ← 关键步骤: 在日程中查看医嘱，
│    并据此排程)    │     根据医嘱进行治疗任务排程
│     ↓            │
│  治疗任务(排程后  │
│    正式创建)     │
│     ↓            │
│  执行治疗         │
│     ↓            │
│  核销确认         │
│     ↓            │
│  工作量统计       │
└──────────────────┘
```

### 6.2 关键依赖关系

| 源模块 | 目标模块 | 关系 | 触发方式 |
|-------|---------|------|---------|
| **治疗方案** | **医嘱** | 治疗方案制定后 → 医生下达医嘱 | 手动提交审阅 |
| **医嘱** | **患者日程** | 医嘱审核通过 → 更新患者日程，治疗师/护士在日程中查看医嘱 | 系统自动 |
| **患者日程** | **治疗任务** | 治疗师/护士在日程中根据医嘱排程 → 正式创建治疗任务 | 人工排程 |
| **治疗任务** | **患者日程** | 任务排程后 → 双向同步更新患者日程视图 | 系统自动 |
| **治疗任务** | **工作量统计** | 任务核销后 → 计入工作量 | 系统自动 |
| **治疗任务(核销)** | **治疗记录** | 核销后 → 新增治疗记录 | 系统自动 |
| **患者医嘱** | **患者列表** | 医嘱变更 → 患者状态标识更新 | 系统自动 |

### 6.3 模块间联动设计要点

1. **医嘱 → 患者日程 → 排程 → 任务**: 医生医嘱审核通过后，先更新患者日程展示医嘱信息；治疗师/护士在患者日程中查看医嘱详情后，根据实际情况手动排程（确认治疗时间段、分配执行人），排程完成后正式创建治疗任务
2. **核销 = 统计源**: 治疗任务核销是工作量统计的唯一数据来源，保证数据一致性
3. **患者列表 = 信息枢纽**: 所有操作从患者列表进入，避免功能入口碎片化
4. **WebSocket 实时推送**: 医嘱变更、任务分配等关键事件实时通知相关角色，避免轮询

---

## 7. 数据库设计概要

### 7.1 核心实体关系（ER 概要）

```
user (用户/员工)
  ├─ 1:N ── task (治疗任务)
  ├─ 1:N ── patient (患者 - 负责治疗师)
  └─ N:N ── therapy_group (治疗小组)

patient (患者)
  ├─ 1:N ── assessment (量表评估)
  ├─ 1:N ── treatment_goal (治疗目标)
  ├─ 1:N ── treatment_plan (治疗方案)
  ├─ 1:N ── medical_order (医嘱)
  ├─ 1:N ── task (治疗任务)
  ├─ 1:N ── treatment_record (治疗记录)
  └─ 1:N ── patient_schedule (患者日程)

medical_order (医嘱)
  ├─ 1:N ── task (治疗任务)
  └─ 1:N ── patient_schedule (日程条目)

task (治疗任务)
  └─ 1:N ── task_verification (核销记录)
  └─ 1:1 ── workload_stat (工作量统计)
```

### 7.2 核心表字段设计要点

#### 用户表 (user)
```
id, username, password, real_name, role(治疗师/护士/医生/管理员),
group_id(FK→therapy_group), phone, email, status, create_time
```

#### 患者表 (patient)
```
id, name, gender, age, inpatient_no, bed_no, admission_date,
diagnosis, allergy_history, attending_therapist_id(FK→user),
status(在院/出院), discharge_date, create_time
```

#### 医嘱表 (medical_order)
```
id, patient_id(FK), doctor_id(FK), plan_id(FK→treatment_plan),
order_type, treatment_item, frequency, period_start, period_end,
note, status(草稿/待审核/已通过/已退回/已作废),
review_comment, create_time, update_time
```

#### 治疗任务表 (task)
```
id, patient_id(FK), order_id(FK→medical_order),
therapist_id(FK→user), group_id(FK), task_date,
treatment_item, time_slot, status(待执行/执行中/已核销/已撤销),
verification_time, revoke_time, revoke_reason, create_time
```

#### 核销记录表 (task_verification)
```
id, task_id(FK), verifier_id(FK→user), verify_time,
revoked(boolean), revoke_time, revoke_reason
```

#### 工作量统计表 (workload_stat)
```
id, user_id(FK), stat_date, treatment_count, patient_count,
treatment_type, create_time
```
> 注: 此表为核销后自动生成的汇总表，也可通过 task 表实时聚合计算。

#### 治疗小组表 (therapy_group)
```
id, group_name, group_leader_id(FK→user), description, create_time
```

#### 量表评估表 (assessment)
```
id, patient_id(FK), template_id(FK→assessment_template), assessor_id(FK→user),
assess_date, total_score, detail(JSON - 各评分项得分),
conclusion, create_time
```

#### 评估量表模板表 (assessment_template)
```
id, template_name, abbreviation, category, items(JSON - 评分项定义),
scoring_rule, max_score, status(启用/停用), create_time
```

#### 治疗目标表 (treatment_goal)
```
id, patient_id(FK), goal_type(短期/长期), content, target_date,
status(进行中/已达成/已放弃), creator_id(FK→user), create_time
```

#### 治疗方案表 (treatment_plan)
```
id, patient_id(FK), plan_name, treatment_items(JSON - 治疗项目列表),
frequency, period_start, period_end, daily_count,
status(草稿/已提交/已审阅), creator_id(FK→user),
reviewer_id(FK→user), review_comment, submit_time, create_time
```

#### 治疗记录表 (treatment_record)
```
id, patient_id(FK), task_id(FK), therapist_id(FK→user),
treatment_date, treatment_item, duration, note, create_time
```

#### 患者日程表 (patient_schedule)
```
id, patient_id(FK), schedule_date, time_slot, event_type(医嘱/治疗任务/评估/其他),
source_id(关联来源ID), therapist_id(FK→user),
title, description, status, create_time
```
> 注: 患者日程为聚合视图，数据来源于医嘱、治疗任务、评估等多张表。可做物理表存储（通过触发器/定时任务同步），也可查询时实时聚合。建议初期用实时聚合，数据量增大后再做物理化。

### 7.3 索引建议

| 表 | 索引字段 | 用途 |
|----|---------|------|
| task | (therapist_id, task_date) | 治疗师按日期查任务 |
| task | (patient_id, task_date) | 患者日程查询 |
| task | (group_id, task_date) | 小组任务查询 |
| medical_order | (patient_id, status) | 待审核医嘱查询 |
| workload_stat | (user_id, stat_date) | 工作量按时间查询 |

---

## 8. 技术架构方案

### 8.1 核心技术栈（完整版）

| 层次 | 技术选型 | 版本建议 | 说明 |
|------|---------|---------|------|
| 后端框架 | Spring Boot | 3.x | 主框架，自动配置 + 内嵌 Tomcat |
| ORM | MyBatis-Plus | 3.5+ | MyBatis 增强，零 SQL 实现基础 CRUD，分页插件内置 |
| 对象映射 | MapStruct | 1.5+ | DTO ↔ Entity 自动映射，编译期生成，性能优于 BeanUtils |
| 代码简化 | Lombok | 1.18+ | @Data / @Builder / @Slf4j 等注解消除样板代码 |
| 工具类库 | Hutool | 5.8+ | Java 万能工具类库（日期/加密/文件/HTTP/JSON 等） |
| 缓存 | Redis | 7.x | 会话缓存 + 热点数据 + 分布式锁 |
| 实时通信 | WebSocket (Spring) | - | 医嘱/任务实时推送通知 |
| 安全框架 | Spring Security + JWT | - | 无状态认证授权 |
| API 文档 | Knife4j (Swagger) | 4.x | 接口文档在线生成与调试 |
| 定时任务 | XXL-JOB | 2.4+ | 分布式定时任务调度（每日治疗任务自动排程提醒、月度统计汇总等） |
| Excel 处理 | EasyExcel | 3.3+ | 工作量报表/患者列表的批量导入导出 |
| 对象存储 | MinIO | - | 量表附件、治疗记录图片存储（初期可暂缓） |
| 数据库 | MySQL | 8.0+ | 主数据库 |
| 连接池 | HikariCP | (Spring Boot 默认) | 高性能连接池 |
| 前端框架 | Vue 3 | 3.4+ | Composition API |
| 状态管理 | Pinia | 2.x | Vue 3 官方推荐状态管理 |
| 路由 | Vue Router | 4.x | SPA 路由 |
| UI 组件库 | Element Plus | 2.x | 企业级组件库（表格/表单/日历/弹窗等） |
| HTTP 客户端 | Axios | 1.x | 请求拦截器 + 统一错误处理 |
| 图表库 | ECharts | 5.x | 工作量统计柱状图/折线图/饼图可视化 |
| 构建工具 | Vite | 5.x | 秒级热更新，快速开发构建 |

### 8.2 技术选型说明

| 技术 | 选型理由 |
|------|---------|
| **MyBatis-Plus** | 替代原生 MyBatis，自带通用 Mapper、分页插件、条件构造器，消灭 90% 的模板 CRUD 代码，是项目效率的核心保障 |
| **Lombok** | Java 开发标配，消除 Getter/Setter/Builder/日志 等样板代码，代码量减少 30%+ |
| **Hutool** | 瑞士军刀级工具库，日期处理（DateUtil）、加密（SecureUtil）、文件操作（FileUtil）等开箱即用 |
| **MapStruct** | 编译期生成映射代码，性能是 BeanUtils 的 10 倍+，尤其适合 Controller 层 DTO ↔ Entity 高频转换 |
| **XXL-JOB** | 分布式任务调度平台。场景：每日凌晨自动向治疗师推送"请排程今日医嘱任务"提醒、每月初自动汇总上月全科工作量统计 |
| **EasyExcel** | 阿里出品，解决 POI 内存溢出痛点，100 万条数据导入导出不 OOM，工作量报表/患者名单导出必备 |
| **ECharts** | 百度出品，柱状图/折线图/饼图/热力图一应俱全，工作量趋势、各治疗类型占比等可视化需求完美匹配 |
| **Knife4j** | Swagger 增强版，自带离线文档导出 + 全局参数配置，前后端联调利器 |

### 8.3 项目结构建议 (后端)

```
rehabilitation-system/
├── rehab-common/              # 公共模块
│   ├── base/                  # 基础类
│   ├── config/                # 全局配置
│   ├── exception/             # 统一异常处理
│   ├── annotation/            # 自定义注解(日志/权限)
│   └── utils/                 # 工具类
├── rehab-modules/
│   ├── rehab-auth/            # 认证授权模块
│   ├── rehab-system/          # 系统管理(管理员)
│   ├── rehab-patient/         # 患者管理
│   ├── rehab-task/            # 治疗任务
│   ├── rehab-order/           # 医嘱管理
│   ├── rehab-assessment/      # 量表评估
│   ├── rehab-treatment/       # 治疗方案
│   ├── rehab-workload/        # 工作量统计
│   └── rehab-websocket/       # WebSocket 服务
└── rehab-api/                 # 对外接口(Controller)
```

### 8.4 项目结构建议 (前端)

```
rehab-web/
├── src/
│   ├── api/                   # API 接口定义
│   ├── assets/                # 静态资源
│   ├── components/            # 公共组件
│   ├── composables/           # 组合式函数
│   ├── layout/                # 布局组件(侧边栏/顶栏)
│   ├── router/                # 路由配置
│   ├── stores/                # Pinia 状态管理
│   ├── views/                 # 页面视图
│   │   ├── therapist/         # 治疗师/护士端
│   │   │   ├── task/          # 治疗任务
│   │   │   ├── patient/       # 患者列表
│   │   │   ├── workload/      # 工作量统计
│   │   │   └── square/        # 功能广场
│   │   ├── doctor/            # 医生端
│   │   └── admin/             # 管理员端
│   ├── utils/                 # 工具函数
│   └── websocket/             # WebSocket 客户端
└── vite.config.ts
```

### 8.5 WebSocket 设计要点

```
连接流程:
  1. 用户登录后建立 WebSocket 连接
  2. 连接时携带 JWT token 进行身份验证
  3. 服务端维护 userId → session 映射
  4. 按角色/小组订阅不同频道

频道设计:
  /topic/task/{userId}         - 个人任务通知
  /topic/task/group/{groupId}  - 小组任务通知
  /topic/order/{patientId}     - 医嘱变更通知
  /topic/system/announcement   - 系统公告

心跳机制:
  - 客户端每 30s 发送 ping
  - 服务端 90s 无心跳则断开
  - 断线自动重连(指数退避)
```

---

## 9. UI/UX 设计参考

### 9.1 整体风格

参考你提供的截图，康复科管理系统应采用以下设计风格：

- **主色调**: 绿色/青绿色系（医疗行业经典色，传递专业、治愈感）
- **配色方案**:
  - 主色: `#1D9E75` (青绿)
  - 辅助色: `#378ADD` (蓝色 - 医嘱/医生相关)
  - 强调色: `#EF9F27` (琥珀 - 提醒/待办)
  - 背景: `#F5F7FA` (浅灰白)
- **字体**: 系统默认中文字体（微软雅黑 / PingFang SC）
- **圆角**: 卡片 12px，按钮 8px

### 9.2 布局结构

```
┌─────────────────────────────────────────────────────┐
│  Logo + 系统名称            🔔通知   👤用户信息       │  ← 顶栏
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│  侧边栏   │              主内容区域                    │
│          │                                          │
│ ■ 治疗任务 │  ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│ ■ 患者列表 │  │ 统计卡片 │ │ 统计卡片 │ │ 统计卡片 │   │
│ ■ 工作量   │  └─────────┘ └─────────┘ └─────────┘   │
│ ■ 功能广场 │  ┌──────────────────────────────────┐   │
│          │  │         数据列表/图表区域           │   │
│ ──────── │  │                                  │   │
│ (医生/   │  │                                  │   │
│  管理员  │  └──────────────────────────────────┘   │
│  额外菜单)│                                          │
└──────────┴──────────────────────────────────────────┘
```

### 9.3 关键组件风格

| 组件 | 风格要求 |
|------|---------|
| 统计卡片 | 白底，左侧彩色竖条标识，大号数字 + 标签文字 |
| 任务卡片 | 浅绿底色，患者名+治疗项目+时间+状态标签 |
| 患者列表 | 表格/卡片双模式切换，行点击展开操作面板 |
| 日历组件 | 日期格内展示任务小圆点，点击跳转当日任务 |
| 模态框 | 居中弹出，操作日志/表单填写场景使用 |
| 标签(Tag) | 状态色区分：待执行=灰、执行中=蓝、已核销=绿、已撤销=红 |

### 9.4 响应式要求

- 优先适配 **1920×1080**（科室台式机主流分辨率）
- 兼容 **1366×768**（部分老旧设备）
- 平板端可做基础适配（非首期优先）

---

## 10. 接口设计概要

### 10.1 RESTful API 规范

```
基础路径: /api/v1

通用规范:
  - 请求头: Authorization: Bearer {jwt_token}
  - 响应格式: { "code": 200, "message": "success", "data": {} }
  - 分页参数: ?page=1&size=20
  - 排序参数: ?sort=create_time,desc
```

### 10.2 核心接口清单

#### 认证模块
```
POST   /api/v1/auth/login              # 登录
POST   /api/v1/auth/logout             # 登出
GET    /api/v1/auth/userinfo           # 获取当前用户信息
PUT    /api/v1/auth/password           # 修改密码
```

#### 治疗任务
```
GET    /api/v1/tasks                        # 任务列表(多条件筛选)
GET    /api/v1/tasks/{id}                   # 任务详情
POST   /api/v1/tasks                        # 排程后正式创建治疗任务
POST   /api/v1/tasks/{id}/verify            # 核销任务
POST   /api/v1/tasks/{id}/revoke            # 撤销核销
GET    /api/v1/tasks/calendar               # 日历视图(按月查询)
PUT    /api/v1/tasks/{id}/schedule          # 排程(设置时间段/分配执行人)
```

#### 患者日程
```
GET    /api/v1/patients/{id}/schedule       # 某患者日程(按日/周/月)
GET    /api/v1/schedule/my                  # 当前用户的日程视图
GET    /api/v1/schedule/group               # 本组日程视图
```

#### 患者管理
```
GET    /api/v1/patients                    # 患者列表
GET    /api/v1/patients/{id}               # 患者详情
POST   /api/v1/patients                    # 新增患者
PUT    /api/v1/patients/{id}               # 编辑患者
PUT    /api/v1/patients/{id}/discharge     # 办理出院
```

#### 医嘱
```
GET    /api/v1/orders                       # 医嘱列表
POST   /api/v1/orders                       # 创建医嘱
PUT    /api/v1/orders/{id}                  # 修改医嘱
PUT    /api/v1/orders/{id}/approve          # 审核通过
PUT    /api/v1/orders/{id}/reject           # 审核退回
PUT    /api/v1/orders/{id}/cancel           # 作废医嘱
```

#### 量表评估
```
GET    /api/v1/assessments                   # 评估列表(按患者)
POST   /api/v1/assessments                   # 新建评估
GET    /api/v1/assessments/{id}              # 评估详情
PUT    /api/v1/assessments/{id}              # 修改评估
```

#### 治疗方案
```
GET    /api/v1/treatment-plans               # 方案列表
POST   /api/v1/treatment-plans               # 创建方案
PUT    /api/v1/treatment-plans/{id}          # 修改方案
POST   /api/v1/treatment-plans/{id}/submit   # 提交审阅
```

#### 工作量统计
```
GET    /api/v1/workload/personal             # 个人统计
GET    /api/v1/workload/group                # 小组统计
GET    /api/v1/workload/department           # 全科统计(管理员)
GET    /api/v1/workload/trend                # 趋势数据(图表用)
GET    /api/v1/workload/export               # 导出报表
```

#### 系统管理（管理员）
```
GET    /api/v1/admin/users                   # 用户列表
POST   /api/v1/admin/users                   # 新增用户
PUT    /api/v1/admin/users/{id}              # 编辑用户
DELETE /api/v1/admin/users/{id}              # 删除用户(软删除)
... (同理: therapy-groups、treatment-items、dict 等)
```

### 10.3 枚举值约定

```
任务状态: PENDING(待执行) / IN_PROGRESS(执行中) / VERIFIED(已核销) / REVOKED(已撤销)
患者状态: IN_HOSPITAL(在院) / DISCHARGED(出院)
医嘱状态: DRAFT(草稿) / PENDING_REVIEW(待审核) / APPROVED(已通过) / REJECTED(已退回) / CANCELLED(已作废)
用户角色: THERAPIST(治疗师) / NURSE(护士) / DOCTOR(医生) / ADMIN(管理员)
```

---

## 11. 开发里程碑建议

### 第一阶段：基础框架搭建（建议 1-2 周）

| 任务 | 产出 |
|------|------|
| 后端项目骨架 (Spring Boot + MyBatis-Plus) | 可运行的空项目 |
| 前端项目骨架 (Vue3 + Element Plus + Vite) | 可运行的空项目 |
| 数据库建表 | 完整 DDL 脚本 |
| 认证模块 (登录/注册/JWT) | 登录功能可用 |
| RBAC 权限框架 | 角色-权限拦截生效 |
| 基础布局 (顶栏+侧边栏+路由) | 三板块框架可切换 |

### 第二阶段：核心业务开发（建议 3-4 周）

| 任务 | 产出 |
|------|------|
| 患者管理 (CRUD + 列表筛选) | 治疗师/管理员可用 |
| 患者 9 项操作中的核心操作 | 详情/编辑/评估/目标/方案 |
| 医嘱模块 (医生端核心) | 创建/审核/退回 可用 |
| 治疗任务模块 (排程+任务创建+核销+撤销) | 完整生命周期可用 |
| 患者日程 (日历视图 + 医嘱查看 + 排程) | 日程与任务联动 |
| WebSocket 实时推送 | 医嘱通知 + 任务分配通知 |

### 第三阶段：统计与完善（建议 1-2 周）

| 任务 | 产出 |
|------|------|
| 工作量统计 (个人/小组/全科) | 可视化图表可用 |
| 功能广场页面 | 入口聚合完成 |
| 管理员全量 CRUD | 字典/用户/小组管理可用 |
| 操作日志 | 审计记录可查 |
| Excel 导出 | 报表导出功能 |

### 第四阶段：联调与优化（建议 1 周）

| 任务 | 产出 |
|------|------|
| 全流程联调测试 | Bug 修复 |
| 性能优化 (缓存/索引/SQL) | 响应 < 500ms |
| UI 细节打磨 | 统一视觉风格 |
| 部署文档 | 上线准备 |

---

## 附录 A：量表示例

康复科常用评估量表（供参考，可按需扩展）：

| 量表名称 | 缩写 | 用途 |
|---------|------|------|
| Fugl-Meyer 运动功能评估 | FMA | 评估中风后运动功能恢复 |
| Barthel 指数 | BI | 日常生活活动能力评估 |
| 改良 Ashworth 量表 | MAS | 肌张力评估 |
| Berg 平衡量表 | BBS | 平衡功能评估 |
| 视觉模拟评分 | VAS | 疼痛评估 |
| Brunnstrom 分期 | - | 偏瘫恢复阶段评估 |

> 量表模块建议设计为**可配置模板**，支持管理员动态添加新的量表模板，每个模板包含若干评分项及计分规则。

---

## 附录 B：关键注意事项

1. **治疗任务的排程与生成**：医嘱审核通过后，首先更新患者日程展示医嘱信息。治疗师/护士在患者日程中查看医嘱详情，手动排程（确认每次治疗的具体时间段和治疗师分配），排程完成后系统根据医嘱频次（如"每日1次，连续7天"）批量生成对应数量的治疗任务，每条任务的 `task_date` 依次递增、`time_slot` 根据排程结果填充。

2. **核销权限校验**：前后端双重校验——前端隐藏非本人的核销按钮，后端接口校验 `task.therapist_id == current_user.id`。

3. **撤销核销的业务约束**：仅有核销记录且未被计入最终统计报表的任务允许撤销（如已进入月度结算则冻结）。

4. **数据一致性**：核销操作涉及 `task` 表状态更新 + `task_verification` 记录插入 + `workload_stat` 更新，建议使用 `@Transactional` 保证事务一致性。

5. **并发控制**：同一任务被多人核销时，使用 Redis 分布式锁或数据库乐观锁（version 字段）防止重复核销。

6. **软删除**：所有删除操作使用 `is_deleted` 字段标记，不执行物理删除，保证数据可追溯。

7. **接口安全**：所有涉及患者隐私数据的接口需要做数据权限校验，确保用户只能查看自己权限范围内的数据。

---

> 本报告基于你提供的需求描述整理，涵盖了系统全部功能模块、角色权限、模块关联、技术架构、UI 风格、接口设计及开发建议。开发过程中如有任何细节需要深化，随时可以针对具体模块展开讨论。
